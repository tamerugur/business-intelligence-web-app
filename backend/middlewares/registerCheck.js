const registerCheckMiddleware = (req, res, next) =>{
    if(req.password != req.repeatPassword){
        // return error message = "passwords do not match!"
    }
    
}


const isValidId = () => {
    
}